---
description: Start project-navigation mode and write a clickable map to work/scratchpad.md.
---

Use the `project-navigator` subagent to handle this request. The navigator
must use the `project-nav` skill and maintain `work/scratchpad.md` as a live
whiteboard with clickable source links, related tests, entry points, and main
call order.

Navigation request:
$ARGUMENTS
